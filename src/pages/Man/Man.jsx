import React from 'react'
import s from './Man.module.scss'
const Man = ({ mon }) => {
  return (
    <div className={s.blok}>
      {/* {mon.map(item => (
        <h1>
          <img src={item.image} alt="" />
          {item.name}
          {item.price}
        </h1>
      ))

      } */}


    </div>
  )
}

export default Man